/**
 * Feature toggles for screen-transition flourishes. Flip to false to fall
 * back to a plain navigation if a transition doesn't feel right on device.
 */
export const SCAN_CUBE_TRANSITION_ENABLED = true;
